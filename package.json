{
  "name": "sentinel-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "packages/sentinel",
    "playground"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=sentinel & npm run dev --workspace=playground",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "clean": "rm -rf node_modules packages/sentinel/node_modules playground/node_modules dist packages/sentinel/dist"
  }
}