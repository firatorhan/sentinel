{
  "name": "sentinel-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "packages/sentinel",
    "packages/sentinel-plugin",
    "playground"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=playground",
    "build": "npm run build --workspace=packages/sentinel && npm run build --workspace=packages/sentinel-plugin",
    "clean": "rm -rf node_modules packages/sentinel/node_modules packages/sentinel-plugin/node_modules playground/node_modules packages/sentinel/dist packages/sentinel-plugin/dist playground/dist package-lock.json"
  }
}